# Backend

Node.js RESTful API sử dụng Express, kết nối PostgreSQL.

## Scripts
- `npm run dev`: Chạy server với nodemon
- `npm start`: Chạy server production

## Docker
Đã cấu hình Dockerfile cho backend.
